# PMC 齐套计算 — 用户操作说明

开发说明：工厂ERP限制过多，流程僵化，不适合PMC物控日常灵活计算，所以自己开发了这款辅助工具，功能简单，胜在灵活度高，经过多次迭代和修改bug，目前使用稳定。

> 适用对象：日常使用大量表格做「物料齐套 / 产能排产」的物控员。
> 软件形态：一个绿色版客户端（PMC 程序），打开即用，数据存在程序旁边的数据库文件里。
> 阅读建议：第一次使用请按本文「推荐操作流程」从上往下走一遍。
> 
> 试用期一年，一年到期会弹出，如果觉得该工具确实能有一定帮助，可赞助一杯奶茶钱38.8元，我会给你一个永久激活码（仅限单台电脑使用）。
> 
> 如需新增功能再根据代码量另行评估。

---

## 〇、推荐操作流程（先记住这个顺序）

```
1) 数据导入  →  把库存 / 在途 / 销售预测 / BOM / 主数据 5 份 Excel 导进来
2) 齐套计算  →  在主计算(整月) 或 临时计算(零散查询) 里算缺料
3) 产能计算  →  录入排产计划，看产能看板 / 产能矩阵 / 物料齐套
4) 数据浏览  →  随时查看、手动维护（新增/编辑/删除）已导入的数据
```

每一步都依赖上一步的数据。例如没导预测就直接算，会提示"勾选的月份无数据"。

---

## 一、数据导入（5 个板块）

> 通用说明：每个导入页都是「选 Excel 文件 → 点导入」。导入成功后页面会显示成功行数、跳过行数，并做一次**数据校验**（绿色=通过，红色=告警但不阻断）。出现告警请留意，通常意味着源表里有脏数据。

### 1. 库存导入

- 入口：左侧「数据导入 → 库存导入」
- 模版：支持两种格式
  - **标准模版**：表头含「物料编码 / 库存地点描述 / 数量」，系统按"库存地点描述"自动归类仓库（材料仓/线边仓/成品仓等）。
  - **工厂模版**：表头含「物料 / 工厂 / 数量」，系统按工厂代码映射仓库类型。
- 注意：**数量含小数会自动四舍五入取整**（页面会黄字提示"本次有 N 行含小数已取整"）。
- 导入后能看到「按仓库类型分布」的行列数。

### 2. 在途导入

- 入口：左侧「数据导入 → 在途导入」
- 模版：《在途模版.xlsx》
- 系统读取三列：**物料 / 采购凭证(PO) / 仍要交货(数量)**。
- 关键点：「仍要交货(数量)」= 真正在途未交量，会在主计算的**最后一个勾选月一次性计入**（不会摊到每个月）。

### 3. 销售预测导入

- 入口：左侧「数据导入 → 销售预测导入」
- 模版：《销售预测模版.xlsx》
- 系统读取「参考编码」作为机型主键，按表头「1月~12月」解析成自然月存储（不含年份）。
- ⚠️ **覆盖式导入**：每次导入会**清空旧预测**再写入。重导前确认无误。

### 4. BOM 导入

- 入口：左侧「数据导入 → BOM 导入」
- 模版：《齐套计算.xlsx》或 ERP 直接导出的单 sheet 齐套 BOM。
- 导入时会**先自动扫描**：若文件内部有重复（同一父件下相同子件出现多次）、或库中已存在该父件，会**弹窗列出明细让你二次确认**——确认「覆盖并导入」才真正写入。
- 导入逻辑是**增量**的：只删除/覆盖你这次导入的机型，不影响别的机型数据。

### 5. 主数据导入

- 入口：左侧「数据导入 → 主数据导入」
- 模版：《主数据.xlsx》
- 系统读取「物料编码 / 物料名称 / 采购员」三列。
- **增量导入**：只新增/更新本文件里的物料，不会清空已有主数据。

---

## 二、齐套计算（3 个板块）

### 1. 主计算（按月整体算缺料）

- 入口：左侧「齐套计算 → 主计算」
- 操作步骤：
  1. **勾选计算月份**：1~12 月任意勾选（可非连续），按勾选顺序累计扣减。
  2. **勾选参与仓库**：默认勾选材料仓/线边仓/成品仓；只有勾选的仓库库存才计入可用量。
  3. **筛选选项**（可选）：
     - 「仅显示缺料行」：结果只列出有缺口的物料。
     - 「仅显示有需求的物料」：过滤掉所有月份需求为 0 的物料。
  4. **特殊需求**（备件/维修机）：点「+ 添加」，填 物料编码 + 月份 + 数量，可随时删除。
  5. 点「**保存配置**」存下勾选；点「**开始计算**」。
- **计算前会先弹「预检结果」**：
  - 警告项（需要处理）：缺 BOM 的机型（需求无法展开会漏算）、BOM 缺主数据的物料（结果缺名称）、切换关系循环引用、自动拆件无 BOM 等——点「补齐数据」去数据浏览修正，或点「忽略并继续计算」强行算。
  - 提示项（可忽略）：勾选的月份/仓库在导入数据里没有内容。
- 结果表格：显示每个物料每月**需求 / 结余 / 在途 / 是否缺料**，红色为缺口。**可点「导出 Excel」保存**；「清空结果」清掉当前表。
- 小提示：结果会在你切换页面再回来时**自动恢复**，不用重算。
- 采购下单建议：需维护住数据的采购交期，会根据采购交期反推合适的下单时间（按月）。

### 2. 临时计算（零散查询，不入库）

- 入口：左侧「齐套计算 → 临时计算」
- 用途：手头有一小批编码要快速看缺不缺料，不想走整月流程时用。
- 操作：
  1. 在文本框里**每行一条**：`编码[Tab或空格]数量`，例如 `A001 100`。
  2. 勾选参与仓库。
  3. 点「开始计算」→ 同样先弹预检 → 出结果（库存/需求/缺料/在途/含在途缺料/备注）。
  4. 点「导出 Excel」下载结果。
- 与主计算区别：**不考虑特殊需求、不逐月累计**，只针对你输入的这批编码展开 BOM 算一次。

### 3. 产能计算（排产模拟，5 个内部页签）

- 入口：左侧「齐套计算 → 产能计算」
- 顶部有 5 个页签：**主计划 / 产能看板 / 产能矩阵 / 物料齐套 / 导出**。

#### 3.1 主计划

- 录入要排产的整机：每行一个机型，填**整机编码 / 总计划数量 / 起始日期 / 截止日期(交期) / 日产能 / PO 号**。
- 先选「参与模拟的仓库」（决定日历矩阵取哪些仓的库存），点「保存仓库选择」。
- 保存后在下方列表管理，可**编辑 / 删除 / 按编码或 PO 搜索**。

#### 3.2 产能看板

- 先在这里**录入每日完成量（实际日产量）**：选整机编码 + 完成日期 + 数量 → 保存。
- 看板每行 = 一条主计划，显示：**计划总数 / 已入库数 / 未交数量**，并可**直接修改「已入库数」**（手改的是修正差额，之后再加每日完成量会在此基础上累加）。
- 顶部可按「月份」筛选。

#### 3.3 产能矩阵

- 选**起始日 ~ 截止日**，填「提前 N 天」（默认 3），点「计算」。
- 系统按工作日展开 BOM、逐日叠加物料用量，预测**库存结余与缺料预警**。
- 可点「工作日设置」调整休息日（默认**周日休息**，可改）。
- 顶部「物料编码」搜索框可筛选只看某个料。
- 图例：🔴 断料（结余<0）/ 🟡 即将缺料（断料前预警）/ 🔴 紧急催货（预警日已过期）。
- 点「导出」下载矩阵 Excel。
- 注：物料很多时（如上千行）表格会自动**分页显示**，用底部「上一页/下一页」翻看，不影响性能。

#### 3.4 物料齐套（按月查缺补漏）

- 点「计算」后，显示：涉及物料总数 / 欠料物料种数 / **整体齐套率**，并配**饼图 + 按采购员欠料柱状图 + 月度欠料概览**。
- 下方明细表：每个物料的需求、月初库存、在途、可用量、**缺口**、满足状态、缺料月份、采购员、备注。
- 选「查看月份」可切换看某月明细；点「导出当月」下载。

#### 3.5 导出

- 一键导出：主计划 / 产能看板 / 产能矩阵 / 物料齐套（全部月份）。
- 注：产能矩阵需先在「产能矩阵」页算过一次才能导出；物料齐套导出全部有需求的月份。

**$4，安全库存报警*$*

 1，主数据有维护安全库存值。

2，库存+在途＜安全库存则报警。

---

## 三、增强功能（4 个子功能）

> 入口：左侧「增强功能」。这些是给计算结果"加标注/做特殊处理"的，不影响库存数字，只在结果里体现。

### 子功能 1 · 切换关系

- 录入「旧码 → 新码」的**切换**或**停产**关系。
- 操作：点「+ 添加切换关系」，填旧码、新码（停产可不填新码），选类型，保存。列表里可删除。
- 两种类型算出来的结果差别很大，下面分开讲（<span style="color:#C62828">**切换关系最容易算糊涂，务必看白话版**</span>）。

#### 类型一：切换（用尽模式，系统默认）

含义一句话：<span style="color:#1565C0">**旧码**</span>先用自己的库存和在途，用光了才轮到<span style="color:#EF6C00">**新码/末码**</span>顶上。

> 🎨 角色颜色：<span style="color:#1565C0">**旧码=要被替换的老料**</span> ／ <span style="color:#EF6C00">**新码/末码=顶上的新料**</span> ／ <span style="color:#C62828">**红色缺口=旧码不够的那部分**</span>

**大白话计算逻辑（重点，别绕进去）：**

每个旧码<span style="color:#1565C0">**独立算自己的账**</span>，不是几个旧码凑一起算。系统对**每一个**旧码各走三步：

1. 算"我手头多少"：该旧码的 **库存 + 在途** 加总。
2. 算"我要用多少"：按预测展开成逐月需求，按月累加起来。
3. 比一比：
   - <span style="color:#2E7D32">**够用**</span>：手头 ≥ 要用的 → 旧码自己搞定，**不拖累末码**，结果里旧码结余为正、正常显示。
   - <span style="color:#C62828">**不够**</span>：手头 < 要用的 → 差多少，就把这<span style="color:#C62828">**缺口**</span>转给末码。

末码（新码）把收到的**所有旧码缺口**加起来，再算自己手头够不够 → 不够就显示缺料。

> ⚠️ **最容易绕进去的误区：**
> 
> - ❌ 误以为"要**所有**旧码都用完了，末码才出现"
> - ✅ 实际是：<span style="color:#1565C0">每个旧码各自独立判断</span>，只要**有一个**旧码缺料，末码就会收到它的缺口、可能被显示出来。其余旧码库存充足的，照常显示、不转缺口。

**真实例子（T01001193 / T01001187 那一组）：**

```
三个旧码都切到同一个末码 T01001187：
  · T01001093：库存1480，三个月需求838 → 够 → 不转缺口
  · T01001185：库存435， 三个月需求300 → 够 → 不转缺口
  · T01001193：库存21，  三个月需求50  → 不够，差29 → 转29给末码
末码 T01001187：自己零库存，收到29缺口 → 显示"缺料29"
```

→ 所以"<span style="color:#2E7D32">只有个别旧码缺料，末码却出现了</span>"是**正常现象，不是 bug**，根源是那个缺料的旧码把缺口甩给了末码。

#### 类型二：停产（stop）

- 旧码只消耗自己的库存，**不采购、不再下单**；库存用光后旧码不再出现在结果里。
- 没有"缺口转新码"这一步，纯粹是旧码自然淘汰。

### 子功能 2 · 半成品自动拆

- 录入要拆的半成品编码，系统按 BOM 递归拆到原料，**拆出量加进原料"总可用量"，父件库存清零**。
- 数量由系统按计算时勾选的仓库库存自动带出，无需手填。
- 与「成品自动拆」是两份独立清单，计算时统一处理。

### 子功能 3 · 普通备注

- 给任意物料编码加自定义备注，计算结果里该编码的「备注」列会显示这段文字（与切换/停产标注合并）。

### 子功能 4 · 成品自动拆

- 同「半成品自动拆」，只是针对成品编码。

---

## 四、数据浏览（5 个页签，查看 + 维护）

> 入口：左侧「数据浏览」。每个页签顶部都显示对应数据的**导入时间**，方便确认数据是否最新。

### 库存 / 在途

- 列表展示已导入的库存、在途数据（默认显示前 200 行，全量请用导出功能）。

### BOM

- 左侧父件列表，**点任一父件**在右侧展开它的子件明细。
- 可「反查」（输入子件编码查它是哪些成品的组成部分）。
- 可「+ 添加子件」直接维护 BOM 关系。

### 销售预测

- 列表展示，可**新增 / 编辑 / 删除**单条预测记录。

### 物料主数据

- 列表展示，可**新增 / 编辑 / 删除**单条物料，也可点「**导出 Excel**」把全部主数据导出来。

---

## 五、通用操作小贴士

- **导出 Excel**：计算/浏览结果里带「导出」按钮的，点一下即下载（浏览器默认下载目录）。
- **编辑/删除**：带「编辑」「删除」按钮的数据（预测、物料、BOM 子件等），点按钮后在弹窗里改。
- **搜索框**：列表/矩阵/主计划等页面顶部有搜索框，输入关键字即时过滤。
- **预检警告别无视**：主计算/临时计算前的预检，红色警告项代表"会漏算或结果缺信息"，最好先去数据浏览补齐再算。
- **数据不同步提醒**：如果你在开发版（直接跑脚本）和正式 exe 之间切换，两边数据库是分开的，同一份数据要各导各的。

---

## 六、自己换电脑 / 重装：把 PMC 程序**和旁边的数据库文件夹、数据才不会丢。

---

> 文档依据软件实际功能界面整理，如后续界面有调整，以实际页面为准。
